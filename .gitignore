data/*
/config/config
!data/cfg_default.json
.node_modules
node_modules
pnpm-lock.yaml
temp/
package-lock.json
.package-lock.json

# 注意：resources/mock 是插件内置的示例数据（含 images/），供指令「日报模拟」预览渲染效果使用，
# 需要随插件一起提交，因此不在此处忽略。
